# answers.sql2

select checknumber , amount and paymentdate from the payments_table;

select orderDate, requiredDate and status_of orders from the orders_table order by catergory DESC,orderDate

select firstName, lastName, and email from the Sales Rep_table order by asc, employeeNumber

select (*) from offices_table;

select productName and quantityInStock from the products_table order by  category asc, buyPrice where output = 5
